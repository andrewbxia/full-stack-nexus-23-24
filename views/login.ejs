<!DOCTYPE html>
<html>
    <head>
        <meta name="description" content="The official login website of Full-Stack Nexus at CRHS, a web development club for beginners!">
        <meta property="og:image" content="assets/icon?type=png">
        <title>login to fullstack.nexus</title>
        <meta name="og:title" content="login to fullstack.nexus">
        <meta name="robots" content="all">
        <link rel="canonical" href="<%= locals.BASE_URL %>/login">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <style>
            html {
                background-color: rgb(200, 232, 240);
                font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Verdana, sans-serif;
            }
            
            body {
                padding:10px;
            }

            input {
                width: 100%;
                padding: 12px 20px;
                margin-bottom: 10px;
                border: 1px solid rgb(200, 232, 240);
                border-radius: 4px;
                box-sizing: border-box;
            }

            button[type=submit] {
                width: 50%;
                background-color: #4CAF50;
                color: white;
                padding: 14px 20px;
                margin: 8px 0;
                border: none;
                border-radius: 4px;
                cursor: pointer;
            }

            #info {
                width: 100%;
                display: flex;
                flex-direction: column;
                align-items: center;
            }

            #titles{
                text-align: center;
                h2 {
                    margin-bottom: 5px;
                }
            }

            #response-message {
                text-align: center;
            }

            #response-message:empty {
                display: none;
            }

            #info-links {
                width: 20%;
                display: flex;
                justify-content: space-between;
                text-align: center;
                margin-bottom: 5px;
                font-size: 1.2em;
                * {
                    padding: 5px;
                    border: 5px solid rgba(0, 0, 0, 0);
                }
                :hover{
                    border: 5px solid black;
                    border-radius: 5px;
                }
            }
        </style>
    </head>
    <body>
        <div id="info">
            <div id="titles">
                <h1>Login Page</h1>
                <h3>Welcome Back!</h3>
                <% if(locals.redirect){ %>
                    <h2>Sent from redirect from /<%= locals.redirect %></h2>
                <% } %>
                <% if(locals.error){ %>
                    <h2 style="color:red;">error ocurred: <%= locals.error %></h2>
                <% } %>
                <% if(locals.message){ %>
                    <h2>reason: <%= locals.message %></h2>
                <% } %>
            </div>
            <p id="response-message"></p>
            <span id="info-links">
                <a href="/join">First Time?</a>
                <a href="/register">Register Page</a>
            </span>
        </div>
        <div>
            <form id="login-form" autocomplete="on">
                <div id="input-section">
                    <input type="username" id="username" placeholder="Username: " type="text" name="username" required autofocus>
                    <input type="password" id="password" placeholder="Password: " type="text" name="password" required>
                </div>
                <div id="submit-section">
                    <button type="submit" id="submit">Login</button>
                </div>
            </form>
        </div>
    </body>
    <script src="../assets/scripts/login.js"></script>
</html>